# BCrypt password hashing strength (valid range: 4-31, higher = more secure but slower)
security.password.bcrypt.strength=10

# Allowed tables for import/export operations
importExport.allowedTables=audit_login_attempts,audit_sessions,audit_dynamic_crud,audit_error_log
