global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  - job_name: 'ssf-graphql'
    static_configs:
      - targets: ['localhost:8080']
    metrics_path: '/actuator/prometheus'
    scrape_interval: 15s
    scrape_timeout: 10s

  # Example for production deployment
  # - job_name: 'ssf-graphql-prod'
  #   static_configs:
  #     - targets: ['your-production-host:8443']
  #   metrics_path: '/actuator/prometheus'
  #   scrape_interval: 30s
  #   scrape_timeout: 10s
  #   tls_config:
  #     ca_file: /path/to/ca.pem
  #     cert_file: /path/to/client.pem
  #     key_file: /path/to/client-key.pem